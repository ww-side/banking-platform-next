import { AuthForm } from '@/core/auth/ui/auth-form';

export default function Auth() {
  return (
    <main className="flex h-screen w-full items-center justify-center">
      <AuthForm />
    </main>
  );
}
